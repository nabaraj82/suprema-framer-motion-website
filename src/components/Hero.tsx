import Navbar from "./Navbar/Navbar";

const Hero = () => {
  return (
    <div className="relative flex flex-col h-screen w-full bg-gradient-radial">
      <Navbar />
          <div className="mt-[90px]">
              hello
     </div>
    </div>
  );
};

export default Hero;
